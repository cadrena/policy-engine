version: "2"

run:
  timeout: 5m

linters:
  default: none
  enable:
    - errcheck
    - godot
    - govet
    - ineffassign
    - revive
    - staticcheck
    - unused

formatters:
  enable:
    - gofmt

issues:
  max-issues-per-linter: 0
  max-same-issues: 0
